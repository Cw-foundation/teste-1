import React from 'react';
import { StyleSheet, View } from 'react-native';

const Pt = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Pt;
